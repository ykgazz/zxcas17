apiVersion: v1
kind: Service
metadata:
  name: shusis
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "shusis"
    port: 8080
  selector:
    app: shusis
